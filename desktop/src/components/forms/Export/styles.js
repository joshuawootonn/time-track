const styles = (theme) => ({
  hero: {
    height: '100vh',
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'center',
  },
  textField: { width: '100%' },
  heroContent: {
    width: '100%',
    maxWidth: '1000px'
  } 
});

export default styles;
