const styles = () => ({
  gridContainer: {
    width: `100% !important`,
    margin: 0
  },
  row: {
    display: `flex`,
    flexDirection: `row`,
    alignItems: `center`,
    justifyContent: `space-between`
  },
  headerRow: {
    minHeight: `72px`
  } 
});

export default styles;
